<core:FragmentDefinition
    xmlns="sap.m"
    xmlns:core="sap.ui.core"
    xmlns:unified="sap.ui.unified">
    <Dialog
        id="massUploadDialog"
        title="{i18n>massUploadDialogTitle}"
        contentWidth="36rem"
        class="sapUiResponsivePadding--header sapUiResponsivePadding--content sapUiResponsivePadding--footer">
        <content>
            <VBox class="sapUiSmallMarginTopBottom">
                <MessageStrip
                    text="{i18n>uploadInstructions}"
                    type="Information"
                    showIcon="true"
                    class="sapUiSmallMarginBottom" />

                <Toolbar design="Transparent">
                    <Button
                        icon="sap-icon://download"
                        text="{i18n>downloadTemplate}"
                        press=".onDownloadTemplate" />
                    <ToolbarSpacer />
                    <Text text="{view>/upload/fileName}" />
                </Toolbar>

                <unified:FileUploader
                    id="massUploadFile"
                    width="100%"
                    fileType="csv"
                    sameFilenameAllowed="true"
                    change=".onMassUploadFileChange" />

                <Text
                    class="sapUiSmallMarginTop"
                    text="{view>/upload/previewText}" />
            </VBox>
        </content>
        <beginButton>
            <Button
                type="Emphasized"
                text="{i18n>upload}"
                press=".onStartMassUpload" />
        </beginButton>
        <endButton>
            <Button
                text="{i18n>cancel}"
                press=".onCancelMassUpload" />
        </endButton>
    </Dialog>
</core:FragmentDefinition>
