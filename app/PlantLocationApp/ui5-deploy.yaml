specVersion: "3.0"
metadata:
  name: plantlocationapp
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: ui5-task-zipper
      afterTask: generateCachebusterInfo
      configuration:
        archiveName: PlantLocationApp
        relativePaths: true
        additionalFiles:
          - xs-app.json
